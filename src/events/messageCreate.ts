/**
 * messageCreate event of the rejoice discord bot trigerred when a new message is created in the guild
 */

export default async (client, message) => {
    // do something on new message creation
}
